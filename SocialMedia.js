import React from 'react';
import { SocialIcon } from 'react-social-icons';

function SocialMediaLink(props){
    return (
    <div>

        <link href="" className="social-media-links">
            <img src={props.image} />
        </link>
    </div>
    );
}

export default SocialMediaLink;